import React from "react";
import ReactDOM from "react-dom";
import Hero from './components/heroBackground'
import App from "./App";

const rootElement = document.getElementById("root");
ReactDOM.createRoot(rootElement).render(<App />);
